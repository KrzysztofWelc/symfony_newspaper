{% extends 'base.html.twig' %}

{% block title %}
    {{ 'comment_delete'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'comment_delete'|trans }}</h1>
    {{ form_start(form, { method: 'DELETE', action: url('comment_delete', {id: comment.id}) }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'comment_delete'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('article_index') }}" class="btn btn-link">
            {{ 'back'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}