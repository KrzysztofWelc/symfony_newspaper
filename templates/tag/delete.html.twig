{% extends 'base.html.twig' %}

{% block title %}
    {{ 'tag_delete'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'tag_delete'|trans }}</h1>
    {{ form_start(form, { method: 'DELETE', action: url('tag_delete', {id: tag.id}) }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'change'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('article_index') }}" class="btn btn-link">
            {{ 'back'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}