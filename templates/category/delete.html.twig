{% extends 'base.html.twig' %}

{% block title %}
    {{ 'categories_delete'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'categories_delete'|trans }}</h1>
    {{ form_start(form, { method: 'DELETE', action: url('category_delete', {id: category.id}) }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'categories_delete'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('category_index') }}" class="btn btn-link">
            {{ 'categories_delete'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}