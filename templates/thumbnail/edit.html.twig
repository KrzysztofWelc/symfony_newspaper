{% extends 'base.html.twig' %}

{% block title %}
    {{ 'article_add'|trans }}
{% endblock %}

{% block body %}
    <div class="card mt-5">
        <div class="card-body">
            <div class="card-title">edytuj miniaturkę</div>

            {{ form_start(form, { method: 'POST', action: url('thumbnail_edit', {"id": id}) }) }}
            {{ form_widget(form) }}
            <div class="form-group row float-sm-right">
                <input type="submit" value="edytuj miniaturkę" class="btn btn-primary" />
            </div>
            <div class="form-group row float-sm-left">
                <a href="{{ url('article_index') }}" class="btn btn-link">
                    zrezygnuj
                </a>
            </div>
            {{ form_end(form) }}

        </div>
    </div>

{% endblock %}