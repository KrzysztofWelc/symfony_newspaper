{% extends 'base.html.twig' %}

{% block title %}
    usuń miniaturkę
{% endblock %}

{% block body %}
    <h1>usuń miniaturkę</h1>
    {{ form_start(form, { method: 'DELETE', action: url('thumbnail_delete', {id: article.id}) }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="usuń miniaturkę" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('article_index') }}" class="btn btn-link">
            {{ 'back'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}