{% extends 'base.html.twig' %}

{% block title %}
    {{ 'article_add'|trans }}
{% endblock %}

{% block body %}
    <div class="card mt-5">
        <div class="card-body">
            <div class="card-title">dodaj miniaturkę</div>

            {{ form_start(form, { method: 'POST', action: url('thumbnail_add', {"id": id}) }) }}
            {{ form_widget(form) }}
            <div class="form-group row float-sm-right">
                <input type="submit" value="dodaj miniaturkę" class="btn btn-primary" />
            </div>
            <div class="form-group row float-sm-left">
                <a href="{{ url('article_index') }}" class="btn btn-link">
                    zrezygnuj
                </a>
            </div>
            {{ form_end(form) }}

        </div>
    </div>

{% endblock %}