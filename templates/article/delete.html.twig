{% extends 'base.html.twig' %}

{% block title %}
    delete
{% endblock %}

{% block body %}
    <h1>usuń</h1>
    {{ form_start(form, { method: 'DELETE', action: url('article_delete', {id: article.id}) }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="usun" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('article_index') }}" class="btn btn-link">
            wróc
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}